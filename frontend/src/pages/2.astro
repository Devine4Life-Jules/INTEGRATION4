---
import { ClientRouter } from "astro:transitions";
import BaseHead from "../components/BaseHead.astro";
import { SITE_TITLE, SITE_DESCRIPTION } from "../consts";
import AbbySmilePng from "../assets/icons/abby-smile-black.png";
import AbbyKortrijkSvg from "../assets/illustrations/abby-kortrijk.svg";
import Tape from "../components/Tape.astro";
import PolaroidStack from "../components/PolaroidStack.astro";
---

<style>
  body {
    height: 100vh;
    display: grid;
    grid-template-rows: min-content 1fr auto;
    align-items: center;
  }

  .container {
    background-color: var(--blue);
    height: 100%;
    display: flex;
    justify-content: center;
    flex-direction: column;
  }

  .title {
    font-size: 3.125rem;
    line-height: 0.8;
    white-space: nowrap;
    margin-top: 0.2em;
  }

  .title__img {
    display: inline;
    width: 3.5rem;
  }

  .title__smaller-text {
    font-size: 1.875rem;
    font-weight: 400;
  }

  .logo {
    align-self: flex-end;
    padding: var(--screen-padding);
    font-variant:;
  }

  @media (orientation: landscape) {
    .container {
      flex-direction: row-reverse;
      justify-items: center;
    }

    .title {
      font-size: 5rem;
      margin: var(--screen-padding);
      margin-top: 1em;
      line-height: 1;
    }

    .title__smaller-text {
      font-size: 5rem;
    }

    .title__img {
      width: 5rem;
    }

    .logo {
      display: flex;
      justify-content: flex-end;
    }
  }
</style>

<html lang="en">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
    <ClientRouter />
  </head>
  <body>
    <Tape text="you are the key" />
    <div class="container">
      <h1 class="title">
        Good job <img
          class="title__img"
          src={AbbySmilePng.src}
          alt="Abby smile icon"
        /><br />
        <span class="title__smaller-text">Story Unlocked</span>
      </h1>
      <PolaroidStack style="transform: scale(0.8);" />
    </div>
    <div class="logo">
      <img class="logo__img" src={AbbyKortrijkSvg.src} alt="Abby icon" />
    </div>
  </body>

  <script>
    import { navigate } from "astro:transitions/client";
    // setTimeout(() => navigate("/3"), 4000);
  </script>
</html>
